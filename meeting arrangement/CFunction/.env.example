# MongoDB 连接配置
# 如果使用默认的本地 MongoDB，可以不设置这些变量

# MongoDB 连接地址（默认: mongodb://49.235.189.246:27017）
MONGO_URI=mongodb://your-mongodb-host:27017

# GHS 数据库名称（会议预约系统，默认: GHS）
DB_NAME=GHS

# GHA 数据库名称（学生信息系统，默认: GHA）
GHA_DB_NAME=GHA

# 服务器端口（默认: 9000）
PORT=9000
